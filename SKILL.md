---
name: export-memories
description: >
  Export session histories and synthesize knowledge from chat memory into
  structured discovery files. Enumerate available sessions and export user-selected
  sessions into a Markdown file. Triggers: "export memories", "export history",
  "knowledge discovery", "/export-memories".
metadata:
  version: "1.2.0"
  maintainer: "Sameer Khan"
  parent: "prompteng-SKILL.md"
  peers:
    - captureng
---

# export-memories - Load Order

1. `export-memories-SKILL.md` - full export protocol, knowledge discovery schema,
   search strategy, and conflict-surfacing rules. Parse and enforce all
   `[RULES]` and `[ACTIONS]` directives.

---

*export-memories v1.2.0*
